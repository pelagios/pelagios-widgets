 <h1 id="{{widgetContext.widgetID}}-title" class="pelagios-title">{{title}}</h1>    
<div class="section-content" id="{{widgetContext.widgetID}}-pleiades">
    <div id="{{widgetContext.widgetID}}-description"><p>{{description}}</p>
    {{#if altNames}}
    <p>Some alternative names: {{altNames}} (the results below are for all alternative names)</p>  
    {{/if}}
    {{#if widgetContext.displayMap}} 
    <div id="{{widgetContext.widgetID}}-map">    
        <div id="{{widgetContext.widgetID}}-map_canvas" style="width:{{widgetContext.mapWidth}}; height:200px"></div>
    </div>   
    {{/if}}
    <p><img src="{{widgetContext.baseURL}}images/icons/pleiades.png"  class="pleiades-icon" />
    <a href="http://pleiades.stoa.org/places/{{pleiadesID}}" target="_blank">Entry in the Pleiades Gazetteer</a></p>    
</div>