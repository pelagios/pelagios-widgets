<div class="section" id="{{widgetContext.widgetID}}-{{name}}">
<div class="section-header" id="{{widgetContext.widgetID}}-header-{{name}}">
<img src="{{iconURL}}" class="section-icon" alt="" />
<h2>{{title}}</h2>
<img class="section-toggle" id="{{widgetContext.widgetID}}-toggle-{{name}}" 
     src="{{widgetContext.baseURL}}images/icons/right-arrow.png" alt="Toggle section"/>
<p class="section-strapline">{{strapline}}</p>
</div>
<div class="section-content" id="{{widgetContext.widgetID}}-content-{{name}}">
</div></div>
